---
name: "Pull Request: Validate"

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

jobs:
  # Pin to main to prevent accidental injection of malicious code
  lint:
    name: Lint
    uses: rafaribe/container-images/.github/workflows/workflow_call-lint.yaml@d5706ecf3533db333e1cd4ae5565ab6ce34813b7 # main

  get-changed-images:
    name: Get changed images
    uses: rafaribe/container-images/.github/workflows/workflow_call-get_changed_images.yaml@d5706ecf3533db333e1cd4ae5565ab6ce34813b7 # main

  build-images:
    name: Build images
    needs:
      - lint
      - get-changed-images
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: rafaribe/container-images/.github/workflows/workflow_call-build_images.yaml@d5706ecf3533db333e1cd4ae5565ab6ce34813b7 # main
    secrets: inherit
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
